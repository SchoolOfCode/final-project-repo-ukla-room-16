// it(`      `, () =>{
//     //Arrange
//     const expected = 

//     //Act   
//     const actual = efficientRoadmap(seco   );
    
//     //Assert
//     expect(actual).toEqual(expected)
    
//     });


test(`check that posts are being fetched`), async () => {
    const posts = await getPosts ({
        username=
        postText=
        createdAt=

    });

    expect(posts.length>0).toBeTruthy();
}